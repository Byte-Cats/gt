package render

// GetCurrentResolution gets current resolution of screen
func GetCurrentResolution() (int, int) {
    return 0, 0
}

type Color struct {
    R, G, B, A float32
}

// DrawRect a rectangle with given color
func DrawRect(x, y, w, h int, color Color) {

}
